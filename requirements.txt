# Phase 1 ingestion
playwright>=1.40
requests>=2.31

# Phase 1.5 metadata
pandas>=2.0

# Phase 2 DPM dictionary (pure-python Access DB reader — no pyodbc/mdbtools needed)
access-parser>=0.0.6

# Phase 2 template titles (EBA Annotated Table Layout)
openpyxl>=3.1

# Zweig B analytics
duckdb>=1.0
pyarrow>=15

# DISDOCS-Korpus (#38): Textausbeute der qualitativen PDF-Berichte messen.
# scripts/probe_disdocs.py importiert es erst beim Messen, damit der Rest der
# Pipeline ohne die Abhängigkeit läuft.
pypdf>=5.0

# Tests (tests/test_xbrl_csv_parser.py — parser regressions: BOM, filing
# indicators, open axis). Run: python -m pytest tests/ -q
pytest>=8.0
